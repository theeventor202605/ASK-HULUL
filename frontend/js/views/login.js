/* Login screen is wired directly in app.js (wireChrome) — this file is kept for structural
   symmetry with the other views and as the place to extend the login screen later
   (e.g. "forgot password", SSO). */
